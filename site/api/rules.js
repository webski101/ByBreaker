// Auto-generated by scripts/generate-site-data.mjs — do not hand-edit.
// Serves a fixed snapshot of the real, reproducible demo run committed at
// examples/sample-run/. Re-run the generator after a fresh `npm run demo`
// to update this.

const DATA = [
  {
    "id": "R1",
    "action": "WARN",
    "metric": "drawdown",
    "description": "WARN if drawdown > 3.00%",
    "source": "WARN IF drawdown > 3%"
  },
  {
    "id": "R2",
    "action": "PAUSE",
    "metric": "drawdown",
    "description": "PAUSE if drawdown > 8.00%",
    "source": "PAUSE IF drawdown > 8%"
  },
  {
    "id": "R3",
    "action": "HALT",
    "metric": "drawdown",
    "description": "HALT if drawdown > 15.00%",
    "source": "HALT IF drawdown > 15%"
  },
  {
    "id": "R4",
    "action": "WARN",
    "metric": "hhi",
    "description": "WARN if hhi > 0.5 over 15 trades",
    "source": "WARN IF hhi > 0.5 OVER 15 TRADES"
  },
  {
    "id": "R5",
    "action": "HALT",
    "metric": "hhi",
    "description": "HALT if hhi > 0.75 over 15 trades",
    "source": "HALT IF hhi > 0.75 OVER 15 TRADES"
  },
  {
    "id": "R6",
    "action": "HALT",
    "metric": "exposurePct",
    "description": "HALT if exposurePct > 80.00% over 15 trades",
    "source": "HALT IF exposurePct > 80% OVER 15 TRADES"
  },
  {
    "id": "R7",
    "action": "PAUSE",
    "metric": "winRate",
    "description": "PAUSE if winRate < 25.00% over 30 trades",
    "source": "PAUSE IF winRate < 25% OVER 30 TRADES"
  },
  {
    "id": "R8",
    "action": "HALT",
    "metric": "dailyLossUsd",
    "description": "HALT if dailyLossUsd > 800",
    "source": "HALT IF dailyLossUsd > 800"
  },
  {
    "id": "R9",
    "action": "KILL",
    "metric": "dailyLossUsd",
    "description": "KILL if dailyLossUsd > 2000",
    "source": "KILL IF dailyLossUsd > 2000"
  },
  {
    "id": "R10",
    "action": "WARN",
    "metric": "tradeFrequency",
    "description": "WARN if tradeFrequency > 10",
    "source": "WARN IF tradeFrequency > 10"
  },
  {
    "id": "R11",
    "action": "HALT",
    "metric": "tradeFrequency",
    "description": "HALT if tradeFrequency > 25",
    "source": "HALT IF tradeFrequency > 25"
  }
];

export default function handler(req, res) {
  res.status(200).json(DATA);
}
